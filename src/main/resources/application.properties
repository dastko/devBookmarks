#Database related properties
database.driverClass=com.mysql.jdbc.Driver
database.url=jdbc:mysql:///test
database.username=root
database.password=root

#Hibernate related properties
hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
hibernate.hbm2ddl.auto=update
hibernate.show_sql=true
hibernate.format_sql=true

#Connection pool related properties
connection.acquireIncrement=2
connection.initialPoolSize=20
connection.minPoolSize=20
connection.maxPoolSize=50
connection.maxIdleTime=600
connection.acquireRetryAttempts=30
connection.acquireRetryDelay=1000
connection.checkoutTimeout=5000
connection.idleConnectionTestPeriod=60
connection.testConnectionOnCheckout=true